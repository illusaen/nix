diff --git a/src/sass/gtk/apps/_gnome-40.0.scss b/src/sass/gtk/apps/_gnome-40.0.scss
index c4f9579..7d60abe 100644
--- a/src/sass/gtk/apps/_gnome-40.0.scss
+++ b/src/sass/gtk/apps/_gnome-40.0.scss
@@ -8,6 +8,12 @@
 $nautilus_borders_color: if($variant=='light', gtkmix(black, $dark_sidebar_bg, 12%), $header_border);
 $flap_sidebar_size: 240px;
 
+// Keep the Nautilus surface tinted enough to remain legible over light windows.
+// It stays slightly translucent and follows libadwaita's light/dark sidebar color.
+$nautilus_background: if($trans == 'true',
+  color-mix(in srgb, gtkcolor(sidebar_bg_color) 98%, transparent),
+  $dark_sidebar_bg);
+
 @if $nautilus_style == 'stable' {
   $nautilus_header_image: linear-gradient(90deg, transparent $flap_sidebar_size,
                                                  $nautilus_borders_color $flap_sidebar_size,
@@ -58,7 +64,7 @@
   @if $nautilus_style == 'stable' or $nautilus_style == 'glassy' {
     &.background.csd {
       &, &:backdrop {
-        background-color: $dark_sidebar_bg;
+        background-color: $nautilus_background;
       }
 
       &:not(.view) headerbar {
