name: Flake checks

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  flake-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            accept-flake-config = true
            experimental-features = nix-command flakes
            max-jobs = auto
            cores = 0

      - name: Check flake outputs
        run: nix flake check --print-build-logs

      - name: Check deploy planning
        run: |
          bin/deploy --plan --on @server
          bin/deploy --plan --on @darwin

      - name: Evaluate host outputs
        run: |
          nix eval --raw .#nixosConfigurations.odin.config.networking.hostName
          nix eval --raw .#lib.fleet.hosts.huginn.targetHost
