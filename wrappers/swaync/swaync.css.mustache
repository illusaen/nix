@define-color theme_fg @base07@; /* Primary Text */
@define-color muted @base04@; /* Secondary Text */
@define-color theme_bg alpha(@base00@, 0.25); /* Panel/Background */
@define-color module_bg alpha(@base00@, 0.6); /* Default Module Background */
@define-color module_hover_bg alpha(@base05@, 0.4); /* Module Hover */
@define-color button_bg alpha(@base03@, 0.3); /* Button Default */
@define-color button_hover_bg alpha(@base04@, 0.4); /* Button Hover */
@define-color accent alpha(@base0E@, 0.8); /* Accent Active */
@define-color accent_hover @base0E@; /* Accent Hover */
@define-color border_light alpha(@base07@, 0.18);
@define-color border_dark alpha(@base00@, 0.06);
@define-color border_medium alpha(@border_light, 0.5);
@define-color shadow alpha(@base00@, 0.5);

/* ── Base Reset ───────────────────────────────────────────────────── */
* {
  font-family: "@font@", sans-serif;
  font-size: 16px;
  color: @theme_fg;
  border: none;
  background: none;
  padding: 0;
  margin: 0;
  box-shadow: none;
  text-shadow: none;
  outline: none;
}

window#control-center,
controlcenter,
notificationwindow,
blankwindow,
window.blank-window,
.blank-window {
  background: transparent !important;
  background-color: transparent !important;
}

/* ── 2. Control Center Container ─────────────────────────────────── */
.control-center {
  background: @theme_bg !important;
  background-color: @theme_bg !important;
  border: 1px solid @border_medium;
  border-top-color: @border_light;
  border-bottom-color: @border_dark;
  padding: 24px;
  min-width: 380px;
}

/* ── 3. Widget Modules ───────────────────────────────────────────── */
.widget-mpris,
.widget-volume,
.widget-backlight,
.widget-buttons-grid,
.control-center-list > box > *:not(.widget-title) {
  background-color: @module_bg;
  padding: 16px;
  margin-bottom: 16px;
  border: 1px solid @border_medium;
}
.widget-volume, .widget-backlight { padding: 16px 24px; }
.widget-buttons-grid { padding: 16px; }
.control-center > box > *:last-child { margin-bottom: 0; }

/* Widget Titles */
.widget-title {
  background: transparent;
  border: none;
  padding: 16px 0;
  margin: 0 8px;
}
.widget-title label {
  font-weight: 500;
  color: @muted;
}
.widget-title button {
  color: @theme_fg;
  background-color: @button_bg;
  padding: 8px 16px;
  transition: background-color 0.15s ease;
}
.widget-title button:hover,
.widget-title button:active {
  background-color: @button_hover_bg;
}

/* ── 4. MPRIS Widget ────────────────────────────────────────────── */
.widget-mpris-album-art {
    /* Hides the album art image */
    display: none;
}
.widget-mpris .widget-mpris-title {
  font-size: 20px;
  font-weight: 600;
  color: @theme_fg;
  margin-bottom: 4px;
  background: none;
}
.widget-mpris .widget-mpris-subtitle {
  font-size: 16px;
  color: @muted;
  background: none;
}
.widget-mpris .widget-mpris-player button {
  all: unset;
  background: transparent;
  padding: 8px;
  margin-bottom: 4px;
  color: @muted;
  font-size: 18px;
  min-width: 36px;
  min-height: 36px;
  transition: background-color 0.15s ease, color 0.15s ease;
}
.widget-mpris .widget-mpris-player button:hover {
  background-color: @button_hover_bg;
  color: @theme_fg;
}
.widget-mpris .widget-mpris-player button:disabled {
  color: alpha(@muted, 0.3);
}

/* ── 5. Sliders (Volume, Backlight) ─────────────────────────────── */
.widget-volume label, .widget-backlight label {
  font-size: 24px;
  color: @theme_fg;
  min-width: 24px;
  margin-right: 16px;
}
scale trough {
  min-height: 8px;
  border-radius: 6px;
}
scale trough progress {
  min-height: 8px;
  border-radius: 6px;
  background-color: @accent;
  transition: background-color 0.15s ease;
}
scale:hover trough {
  background-color: alpha(@accent, 0.5);
}
trough highlight {
  min-height: 8px;
  padding: 0;
  background: @accent;
  border-radius: 6px;
  box-shadow: 0 0 5px @shadow;
  transition: all 0.7s ease;
}
scale slider {
  min-width: 14px;
  min-height: 14px;
  margin: -3px;
  border-radius: 7px;
  background-color: @theme_fg;
}

/* ── 6. Button Grid ─────────────────────────────────────────────── */
.widget-buttons-grid > flowbox {
  padding: 0;
  background: none;
}
.widget-buttons-grid > flowbox > flowboxchild {
  padding: 3px;
  background: none;
}
.widget-buttons-grid > flowbox > flowboxchild > button {
  padding: 16px;
  min-width: 36px;
  min-height: 36px;
  color: @theme_fg;
  transition: background-color 0.15s ease, color 0.15s ease;
}
.widget-buttons-grid > flowbox > flowboxchild > button:hover,
.widget-buttons-grid > flowbox > flowboxchild > button.toggle:checked:hover {
  background-color: @accent_hover;
  color: @theme_bg;
}
.widget-buttons-grid > flowbox > flowboxchild > button.toggle:checked {
  background-color: @accent;
  color: @theme_bg;
}


/* ── 7. Notification Center ─────────────────────────────────────── */
.control-center-list {
  padding-bottom: 1px;
  background-color: transparent;
}
.control-center .notification-row {
  margin-bottom: 12px;
}
.control-center .notification-background {
  background-color: @module_bg;
  border: 1px solid @border_medium;
  padding: 16px;
  border-radius: 18px;
  transition: background-color 0.15s ease;
}
.control-center .notification-row:hover .notification-background {
  border-radius: 18px;
  background-color: @module_hover_bg;
}
.control-center .notification .summary {
  font-weight: 600;
  color: @theme_fg;
  margin-bottom: 5px;
}
.control-center .notification .body {
  color: @muted;
}
.control-center .notification .time {
  font-size: 15px;
  font-weight: 500;
  color: @muted;
  margin-left: 12px;
}
.control-center .close-button {
  background-color: @button_bg;
  border-radius: 50%;
  min-width: 24px;
  min-height: 24px;
  transition: background-color 0.15s ease;
}
.control-center .close-button:hover {
  background-color: @button_hover_bg;
}
.control-center .close-button image {
    margin-right: 12px; /* Adjust this value for desired spacing */
  color: @muted;
}

.control-center .notification image {
  margin-right: 12px; /* Space between image and text */
  min-width: 36px;    /* Define a minimum width */
  min-height: 36px;   /* Define a minimum height */
  /* Add or adjust the border-radius property here */
  border-radius: 8px; /* <<< THIS LINE controls the rounding */
}
/* ── 8. Floating Notifications ─────────────────────────────────── */
.notification-row .notification-content .summary,
.notification-row .notification-content .body {
  /* Forces the text to wrap to the next line instead of trailing off */
  word-wrap: break-word;
  word-break: break-word;
  /* Allows the label to take up more vertical lines */
  white-space: normal;
}

.floating-notifications .notification-row {
  margin-bottom: 12px;
}
.floating-notifications .notification-background {
  background-color: @module_bg;
  border: 1px solid @border_light;
  border-radius: 18px;
  padding: 18px;
  margin: 8px 14px;
  transition: background-color 0.15s ease;
}
.floating-notifications .notification-row:hover .notification-background {
  background-color: alpha(@module_bg, 1.1);
}
.floating-notifications .notification .summary {
  font-weight: 700;
  color: @theme_fg;
}
.floating-notifications .notification .body {
  color: @theme_fg;
}
.floating-notifications .notification .time {
  font-size: 15px;
  color: @muted;
  margin-left: 10px;
}
.floating-notifications .notification image {
  min-width: 48px;
  min-height: 48px;
  margin-right: 14px;
  border-radius: 10px;
}
.floating-notifications .close-button {
  background-color: @button_bg;
  border-radius: 50%;
  min-width: 28px;
  min-height: 28px;
  transition: background-color 0.15s ease;
}
.floating-notifications .close-button:hover {
  background-color: @button_hover_bg;
}
.floating-notifications .close-button image {
    margin-right: 12px; /* Adjust this value for desired spacing */
  color: @muted;
}
.floating-notifications .notification-alt-actions {
  margin-top: 16px;
  padding-top: 16px;
  border-top: 1px solid @border_medium;
}
.floating-notifications .notification-action button,
.floating-notifications .notification-alt-actions button {
  font-size: 16px;
  color: @theme_fg;
  background-color: @button_bg;
  border-radius: 12px;
  padding: 8px 18px;
  transition: background-color 0.15s ease;
}
.floating-notifications .notification-action button:hover,
.floating-notifications .notification-alt-actions button:hover {
  background-color: @button_hover_bg;
}
